{
  "name": "semcomp24-server",
  "version": "1.0.0",
  "description": "Game server used in the semcomp 24 (2021)",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=dev nodemon ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Semcomp24",
  "license": "",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
